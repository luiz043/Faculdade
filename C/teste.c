#include <stdio.h>
#include <stdlib.h>
int main ()
{
    int l, c, n;
    int mat [3][3];
    int v[10];
    for(l=0; l<3; l++)
    {
        for(c=0; c<3; c++)
        {
            scanf("%d", &c);
        }
        scanf("%d", &l);
    }
    for(l=0; l<3; n++)
    {
        for(c=0; c<3; c++)
        {
            if(mat[l][c] == ){
                
            }
        }
    }
}