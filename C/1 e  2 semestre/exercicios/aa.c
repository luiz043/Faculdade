#include <stdio.h>
#include <stdlib.h>
int main()
{
    int soma,num=1,cont=0;
        if (num/num==1)
            {
                soma=1;
                soma=(num+soma);
                cont++;
            
            }
        printf(soma);

    return 0;
}

